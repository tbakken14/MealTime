@using MealTime.Models

@{
    Layout = "_Layout";
}

<h2>Recipes</h2>
@Html.ActionLink("New Recipe", "Create")

@if (Model.Count == 0)
{
    <p>No recipes</p>
}
@foreach (Recipe recipe in Model)
{
    <p>Model.Name</p>
}